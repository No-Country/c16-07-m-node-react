export type TUser = {
    readonly userId: number;
    // achievements: <pending>
    aboutMe: string;
    birthdate: Date;
    country: string;
    createdAt: Date;
    email: string;
    // interests: <pending>
    firstName: string;
    lastName: string;
    observations: string;
    password: string;
    postalCode: string;
    // purpose: <pending>
    updatedAt: Date;
    address?: string;
};